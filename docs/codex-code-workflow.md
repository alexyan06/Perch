# Codex Workflow Notes

This repo uses `.codex/` for local agent configuration.

- Read `CODEX.md` first for project-wide rules.
- Use `.codex/skills/` for subsystem-specific guidance.
- Keep edits narrow and run `npm run typecheck` before treating a task as done.

